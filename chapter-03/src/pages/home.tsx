import { Outlet } from "react-router-dom"
//import Navbar from "../components/navbar";
//home.tsx
const HomePage = () => {
    return (
        <div>
            <Outlet/>
        </div>
    );    
};

export default HomePage